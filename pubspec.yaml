name: flutter_todo
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.0.2

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.1
  realm: ^1.6.1
  path_provider: ^2.1.1
  url_launcher: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  msix: ^3.16.7

msix_config: 
  display_name: Flutter App
  publisher_display_name: Andres Mosquera
  identity_name: test.app
  msix_version: 1.0.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/config/
